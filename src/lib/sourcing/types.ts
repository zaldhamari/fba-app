// Shared types used across all Phase 13 sourcing intelligence engines.

/** Confidence in an engine's output given available data. */
export type DataConfidence = 'High' | 'Medium' | 'Low' | 'Unknown';
